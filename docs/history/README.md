# docs/history/

Archived planning artefacts from earlier iterations of skill-manager.

These files are retained for context (what we decided, what we rejected,
what we postponed) but are **superseded** by the documents in
`docs/specs/`. If anything here contradicts a current spec, the current
spec wins.

| File | Originally written | Superseded by |
| --- | --- | --- |
| `IMPLEMENTATION_PLAN.md` | v0.2–v0.3 era; tracked the per-phase work towards the dashboard + MCP support. Lapsed mid-Phase-1. | `docs/specs/v0.4-overhaul.md` |
| `skill-manager-v2-plan.md` | v0.3 era; sketched the "v2 platform" pivot. Some ideas (unified Resource model, Port interfaces) survive in v0.4; the v2 versioning never happened. | `docs/specs/v0.4-overhaul.md` |

Do not edit these files — open a new spec under `docs/specs/` instead.
